/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export',
  // basePath: '/cmu-modelverse-initiative-light',
  images: {
    unoptimized: true,
  },
};

export default nextConfig;
